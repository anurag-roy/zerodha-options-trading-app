{
  "name": "zerodha-options-trading-app",
  "version": "1.0.0",
  "description": "App to trade in options.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf build && cd options-ui && npm run build --prod && cp -r build ../",
    "start": "node socket.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anurag Roy",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "kiteconnect": "^3.1.0",
    "socket.io": "^2.3.0"
  }
}
